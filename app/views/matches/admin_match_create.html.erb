<div class="newMatch">
	<h1>Admin Report a Match</h1>
	<%= error_messages_for :match %>
	<% form_for @match do |f| %>
		
		<div class="opponent">
			Home: <%= f.select :home, other_users(current_user).collect { |u| ["#{u.login} (#{u.email})", u.id] } %>
		</div>
		<div class="opponent">
			Away: <%= f.select :away, other_users(current_user).collect { |u| ["#{u.login} (#{u.email})", u.id] } %>
		</div>
		
		<table class="scores">
			<tr>
				<td>
					Your Score
				</td>
				<td>
					Their Score
				</td>
				<td>
					K
				</td>
			</tr>
			<tr>
				<td>
					<%= f.text_field :home_score %>
				</td>
				<td>
					<%= f.text_field :away_score %>
				</td>
				<td>
					<%= f.text_field :k%>
				</td>
			</tr>
		</table>
		<%= submit_tag "Record Match", :confirm => "Please be sure to check the match entry for accuracy.", :class => "recordMatch" %>
	<% end%>
</div>